<Project Sdk="Microsoft.NET.Sdk">

  <!-- Local-files source plugin. References the host (Core) for shared playback infra (playlist,
       decoders, the pausing pump) + the SDK (Abstractions, transitively). First source extracted
       into its own assembly — the pattern the other sources follow. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\HorizonRadio.Core\HorizonRadio.Core.csproj" />
  </ItemGroup>

</Project>
