<Project Sdk="Microsoft.NET.Sdk">

  <!-- yt-dlp tool plugin: owns its own provisioning (download + verify) end to end. References Core
       for the generic install pipeline (ToolInstallerBase) + the SDK contracts (transitively). -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\HorizonRadio.Core\HorizonRadio.Core.csproj" />
  </ItemGroup>

</Project>
