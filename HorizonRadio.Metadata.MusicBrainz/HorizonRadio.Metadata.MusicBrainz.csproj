<Project Sdk="Microsoft.NET.Sdk">

  <!-- MusicBrainz metadata plugin. References the host (Core) for shared helpers + the SDK
       (Abstractions, transitively). No InternalsVisibleTo — nothing internal is unit-tested. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\HorizonRadio.Core\HorizonRadio.Core.csproj" />
  </ItemGroup>

</Project>
