# MSBuild outputs (we route all artifacts under build/ via OutDir/IntDir)
build/
bin/
obj/


# Local dev helper: builds Release + copies version.dll into the game
# folder. Personal (machine-specific game path), not for the repo.
deploy.ps1

# Visual Studio / Rider local state
.vs/
.idea/
*.user
*.suo
*.aps
*.opensdf
*.sdf
ipch/
*.VC.db
*.VC.opendb
riderModule.iml
/_ReSharper.Caches/
/packages/

# Compiler / linker intermediates
*.obj
*.pdb
*.ilk
*.exp
*.lib

# Horizon Radio: user-supplied music + runtime trigger files are
# kept out of the repo to respect copyright and avoid accidental
# commits of personal libraries.
music/
playlist.m3u
playlist.m3u8
test.mp3
test.wav
test.flac
test.ogg
horizon-radio.discover

# OS noise
Thumbs.db
.DS_Store
desktop.ini

# Stray tool config written to a literal ~ instead of $HOME
/~/
